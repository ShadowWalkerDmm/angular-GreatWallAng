export interface ResponseNr {
    idDoor ? : number,
    idWindow ?: number,
    idUser ? : number,
    doorState ?: string,
    windowState ? : string,
    motion ?: string,
    smoke ?: string,
    water ?: string,
    timestamp: string
    

}